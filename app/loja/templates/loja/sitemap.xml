<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for page in pages %}
    <url>
        <loc>{{ page.loc }}</loc>
        {# Se puder passar a data real do banco de dados aqui, o SEO agradece #}
        <lastmod>{{ now_local().strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>daily</changefreq>
        {# Lógica de prioridade: Home(1.0) > Categorias(0.8) > Produtos(0.6) #}
        <priority>
            {% if '/produto/' in page.loc %}0.6
            {% elif '/categoria/' in page.loc %}0.8
            {% else %}1.0{% endif %}
        </priority>
    </url>
    {% endfor %}
</urlset>