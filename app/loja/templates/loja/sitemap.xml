<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for page in pages %}
    <url>
        <loc>{{ page.loc }}</loc>
        <lastmod>{{ now_local().strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>daily</changefreq>
        <priority>{{ '1.0' if loop.first else '0.8' }}</priority>
    </url>
    {% endfor %}
</urlset>